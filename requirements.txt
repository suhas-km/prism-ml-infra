# =============================================================================
# ML Platform Requirements
# =============================================================================
# IMPORTANT: For ARM/aarch64 systems (e.g., NVIDIA GH200), install PyTorch first:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
# Then install these requirements.
# =============================================================================

# Core dependencies
ray[serve]>=2.9.0
vllm>=0.11.0
fastapi>=0.109.0
uvicorn>=0.27.0
pydantic>=2.5.0
packaging>=21.0

# Training dependencies
transformers>=4.38.0
datasets>=2.18.0
peft>=0.9.0
trl>=0.7.0
accelerate>=0.27.0
bitsandbytes>=0.42.0

# ML dependencies (torch installed separately for CUDA on ARM)
sentencepiece>=0.2.0

# Logging
structlog>=24.1.0

# Utilities
python-dotenv>=1.0.0
